Project is simple calculator, composed from node js backend and react frontend
It is deploying to docker by docker-compose

You must have Docker installed.

Run from root folder terminal:
docker-compose up -d
